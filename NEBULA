local _s = "local _P=[[-- NEBULA UI FULL SCRIPT PAYLOAD --\nlocal CoreGui = game:GetService('CoreGui'); local Players = game:GetService('Players'); local RunService = game:GetService('RunService'); local UserInputService = game:GetService('UserInputService'); local TweenService = game:GetService('TweenService'); local LocalPlayer = Players.LocalPlayer; local Camera = workspace.CurrentCamera; local ScreenGui = Instance.new('ScreenGui'); ScreenGui.Name = 'NebulaInspectorUI'; ScreenGui.Parent = CoreGui; ScreenGui.DisplayOrder = 999999; ScreenGui.ResetOnSpawn = false; local FovContainer = Instance.new('Frame'); FovContainer.Name = 'FovContainer'; FovContainer.Size = UDim2.new(0, 260, 0, 260); FovContainer.Position = UDim2.new(0.5, 0, 0.5, 0); FovContainer.AnchorPoint = Vector2.new(0.5, 0.5); FovContainer.BackgroundTransparency = 1; FovContainer.Visible = false; FovContainer.Parent = ScreenGui; print('Nebula UI Loaded Successfully!')]]; loadstring(_P)()"
loadstring(_s)()
